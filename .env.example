# AWS Configuration
AWS_REGION=us-east-1
AWS_PROFILE=default

# Bedrock Configuration
BEDROCK_KB_ID=your-knowledge-base-id-here
BEDROCK_MODEL_ID=anthropic.claude-3-sonnet-20240229-v1:0

# Backend Configuration
BACKEND_PORT=8000
LOG_LEVEL=INFO

# Rate Limiting Configuration
RATE_LIMIT_ENABLED=true
RATE_LIMIT_STORAGE=memory  # "memory" for dev, "redis" for production
REDIS_URL=  # Redis connection URL (required for production, e.g., redis://localhost:6379/0)
RATE_LIMIT_REVIEW=10/minute
RATE_LIMIT_METRICS=60/minute
RATE_LIMIT_GRAPH=30/minute

# Frontend Configuration (for local dev)
NEXT_PUBLIC_API_URL=http://localhost:8000

# DO NOT PUT REAL CREDENTIALS HERE
# Use local .env (git-ignored) or AWS Secrets Manager
