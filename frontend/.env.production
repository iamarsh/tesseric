# Production environment variables for Vercel deployment
# Backend URL pointing to Railway deployment
NEXT_PUBLIC_API_URL=https://tesseric-production.up.railway.app

# Backend is deployed on Railway with auto-deploy from main branch
# CORS is configured to allow requests from tesseric.ca and www.tesseric.ca
