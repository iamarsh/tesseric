# Production environment variables for Vercel deployment
# Backend URL - update this when backend is deployed
NEXT_PUBLIC_API_URL=https://api.tesseric.ca

# Note: Until backend is deployed, the frontend will use client-side
# fallback analysis. This provides basic AWS pattern matching without
# requiring the backend to be available.
